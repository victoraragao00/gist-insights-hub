# AUDITORIA COMPLETA — 2026-03-22

> Executada por: Claude Code (Agente de Revisão e Engenharia)
> Escopo: Checklist CTO (m1-m13) + Anti-padrões PRD/Playbook + Design System + Segurança
> Versão do CONTEXT.md: v21 (2026-03-19)

---

## Resumo Executivo

| Categoria | Status | Críticos | Médios | Baixos |
|-----------|--------|----------|--------|--------|
| Checklist CTO (m1-m13) | ⚠️ | 1 | 3 | 1 |
| Anti-padrões PRD/Playbook | ⚠️ | 1 | 2 | 0 |
| Design System | ⚠️ | 0 | 3 | 2 |
| Segurança | ⚠️ | 2 | 1 | 0 |
| **Total** | **⚠️** | **4** | **9** | **3** |

---

## 1. Checklist do CTO (m1-m13)

### m1: Zero `any` fora de UI — ⚠️ FAIL (7 violações)

Uso de `as unknown as Type` em hooks e lógica (equivalente funcional a `any`):

| # | Arquivo | Linha | Detalhe |
|---|---------|-------|---------|
| 1 | `src/hooks/useDemands.ts` | 87 | `return (data as unknown) as DemandRow[]` |
| 2 | `src/hooks/useClientDemands.ts` | 35 | `return (data ?? []) as unknown as ClientDemand[]` |
| 3 | `src/hooks/useDemandAnalytics.ts` | 53 | `const result = data as unknown as DemandAnalyticsData` |
| 4 | `src/hooks/useDemandInteractions.ts` | 56 | `return (data ?? []) as unknown as LinkedInteraction[]` |
| 5 | `src/components/demands/ColumnSettingsTab.tsx` | 166 | `(err as unknown as ColumnHasTicketsError).code` |
| 6 | `src/components/demands/ColumnSettingsTab.tsx` | 167 | `(err as unknown as ColumnHasTicketsError).count` |
| 7 | `src/pages/ClientDetailPage.tsx` | 328 | `(clientDemands as unknown as DemandRow[])` |

**Recomendação:** Usar tipagem Supabase nativa (`Database` types) ou type guards explícitos.

### m2: Error boundaries em toda rota — ✅ PASS

Todas as rotas protegidas possuem `<ErrorBoundary>` wrapper em `App.tsx`. Componente em `src/components/ErrorBoundary.tsx`.

### m3: Toast único (sonner) — ✅ PASS

Apenas `sonner` usado. Nenhum `use-toast`, `useToast` ou `<Toaster>` duplicado.

### m4: staleTime > 0 — ✅ PASS

Todas as 25+ queries com staleTime correto. Estáveis = 300s (5min), dinâmicos = 30-60s.

### m5: queryKey completo — ✅ PASS

Todas as queryKeys incluem dependências relevantes (filtros, IDs, paginação).

### m6: useRef, não DOM IDs — ✅ PASS

Zero IDs estáticos no DOM fora de componentes framework-managed.

### m7: Guard contra chamadas duplas — ✅ PASS

Todos os handlers críticos com `isPending` ou `disabled` guard.

### m8: Erros Supabase tratados — ✅ PASS

Todos os `{ data, error }` destructurados e error tratado em 30+ hooks e componentes.

### m9: useMutation para escrita — ⚠️ FAIL (1 violação)

| # | Arquivo | Linha | Detalhe |
|---|---------|-------|---------|
| 1 | `src/components/GistContactWizard.tsx` | 304-354 | `setSaving(true/false)` manual em vez de `useMutation` |

### m10: refetch() não descartado — ✅ PASS

Nenhum `refetch()` encontrado. Usa `invalidateQueries` consistentemente.

### m11: Zero imports não usados — ✅ PASS

Nenhum import não utilizado encontrado.

### m12: Paginação em listas > 50 — ⚠️ FAIL (1 violação)

| # | Arquivo | Linha | Detalhe |
|---|---------|-------|---------|
| 1 | `src/components/InteractionsFeed.tsx` | 463-477 | Query sem `.range()` ou `.limit()` — pode retornar milhares de linhas |

Todas as demais listagens (ClientsPage, ClientDetailPage, AuditRules) usam `PAGE_SIZE = 50` com `.range()`.

### m13: Testes em caminhos críticos — ⚠️ PARCIAL

| Caminho | Teste | Status |
|---------|-------|--------|
| `calculate-priority-scores` (logic.ts) | `index.test.ts` | ✅ |
| `evaluate-audit-rules` | `index.test.ts` | ✅ |
| `useUserRole` | `priorityDashboard.test.ts` | ✅ |
| Priority Dashboard logic | `priorityDashboard.test.ts` | ✅ |
| **process-jobs** | — | ❌ Sem teste |
| **classify-batch** | — | ❌ Sem teste |
| **ClientContext.tsx** | — | ❌ Sem teste |
| **Interactions listings** | — | ❌ Sem teste |

---

## 2. Anti-padrões PRD / Playbook

### CRÍTICO

| # | Arquivo | Linha | Anti-padrão | Detalhe |
|---|---------|-------|-------------|---------|
| AP1 | `supabase/functions/process-jobs/index.ts` | 380 | DELETE sem filtro `auto_created` | `delete().eq('client_id', deleteClientId)` apaga TODAS as interactions do cliente — risco de perda de dados reais |

### MÉDIO

| # | Arquivo | Linha | Anti-padrão | Detalhe |
|---|---------|-------|-------------|---------|
| AP2 | `supabase/functions/evaluate-audit-rules/index.ts` | 166 | INSERT sem ON CONFLICT | `.insert({ rule_id, client_id, ... })` pode falhar em duplicata |
| AP3 | `supabase/functions/gist-confirm-mapping/index.ts` | 77,91,116,162,201,241 | Múltiplos INSERTs sem conflict handling | Re-execução pode gerar duplicatas |

### LIMPO

- ✅ Zero uso de OpenAI
- ✅ Zero hardcoded credentials (todos via `Deno.env.get`)
- ✅ Zero referências a `~/Desktop/CX HUB/`
- ✅ Zero arquivos `.txt` em docs/auditorias
- ✅ `localStorage` usado apenas para bootstrap flag (sessionStorage) — aceitável
- ✅ UPSERTs com `onConflict` em process-jobs (linhas 203, 337, 500)

---

## 3. Design System

### MÉDIO

| # | Arquivo | Linha | Violação |
|---|---------|-------|----------|
| DS1 | `src/pages/DemandsDashboardPage.tsx` | 26-31 | PRIORITY_COLORS com HSL hardcoded em vez de tokens semânticos |
| DS2 | `src/pages/ClientDetailPage.tsx` | 668-671 | Tone colors em chart com HSL hardcoded |
| DS3 | `src/pages/ClientDetailPage.tsx` | 698 | `bg-green-500` em vez de `bg-emerald-500` (Design System spec) |

### BAIXO

| # | Arquivo | Linha | Violação |
|---|---------|-------|----------|
| DS4 | `src/components/ui/skeleton.tsx` | 4 | `animate-pulse` em vez de `animate-shimmer` (DESIGN_SYSTEM.md §2.1) |
| DS5 | Múltiplos | — | Zero uso de `motion-safe:` prefix (DESIGN_SYSTEM.md §2.4 — acessibilidade) |

### LIMPO

- ✅ 5 keyframes custom registrados em `tailwind.config.ts`
- ✅ Dark mode com variantes semânticas
- ✅ Typography e spacing consistentes
- ✅ `tabular-nums` em valores numéricos

### RECOMENDAÇÃO

Criar `src/lib/colorPalette.ts` centralizando constantes de cor (tone, tier, score, severity) para eliminar HSL hardcoded em 3+ páginas. Também consolidar `TONE_CONFIG` e `SIDE_BADGE` duplicados.

---

## 4. Segurança

### CRÍTICO

| # | Arquivo | Linha | Vulnerabilidade |
|---|---------|-------|-----------------|
| SEC1 | `supabase/functions/gist-discover/index.ts` | 46 | Sem validação JWT + `Access-Control-Allow-Origin: *` — endpoint aberto com service_role |
| SEC2 | `supabase/functions/gist-proxy/index.ts` | 1, 32-42 | Sem validação JWT + CORS aberto + sem sanitização de `params` antes de URLSearchParams |

### MÉDIO

| # | Arquivo | Linha | Vulnerabilidade |
|---|---------|-------|-----------------|
| SEC3 | `supabase/functions/gist-proxy/index.ts` | 32-42 | Params passados diretamente para Gist API sem validação — possível injection |

### LIMPO

- ✅ `client-demands-public` NÃO expõe campos sensíveis (notes, description, rfi_url, blocker_reason, created_by)
- ✅ `bootstrap-user-access` e `sync-gist-contacts` validam JWT corretamente
- ✅ ProtectedRoute redireciona para /login se user null
- ✅ Zero secrets hardcoded (todos via env vars)
- ✅ `.env` contém apenas chaves públicas (VITE_* anon key)

---

## 5. Pendências do Cowork (PENDENTES.md — 2026-03-08)

Verificação do estado atual das 8 pendências anteriores:

| ID | Descrição | Status anterior | Status atual |
|----|-----------|-----------------|--------------|
| A1 | SearchPage.tsx TDZ | ABERTO | **RESOLVIDO** — Issue #65 fechada |
| A2 | Audits.tsx `key={i}` em lista dinâmica | ABERTO | **A VERIFICAR** |
| A3 | CONTEXT.md Issue #13 inconsistente | ABERTO | **RESOLVIDO** — CONTEXT.md v21 corrigido |
| O1 | useAuditRules `alert_recipients: unknown` | ABERTO | **A VERIFICAR** |
| O2 | TONE_CONFIG duplicado em 3 páginas | ABERTO | **CONFIRMADO** — ainda duplicado |
| O3 | queryKey sem `limit` | ABERTO | **BAIXO RISCO** — impacto zero |
| O4 | Agente "Projeto" na Colaboração | ABERTO | **INFORMACIONAL** |
| O5 | Changelog v8→v9 ausente | ABERTO | **INFORMACIONAL** |

---

## 6. Plano de Ação Recomendado

### Prioridade CRÍTICA (corrigir imediatamente)

1. **SEC1+SEC2:** Adicionar validação JWT a `gist-discover` e `gist-proxy` (ou restringir CORS a origem específica)
2. **AP1:** Adicionar filtro `metadata->>'auto_created' = 'true'` ao DELETE em `process-jobs/index.ts:380`

### Prioridade ALTA

3. **AP2:** Adicionar `onConflict` ao INSERT de `audit_alerts` em `evaluate-audit-rules`
4. **AP3:** Adicionar conflict handling aos INSERTs de `gist-confirm-mapping`
5. **m12:** Adicionar `.range()` ou `.limit()` à query de `InteractionsFeed.tsx:468`

### Prioridade MÉDIA

6. **m1:** Substituir 7x `as unknown as Type` por tipagem Supabase nativa nos hooks
7. **m9:** Refatorar `GistContactWizard.tsx:handleSave` para usar `useMutation`
8. **DS1-DS3:** Centralizar paleta de cores e corrigir `green-500` → `emerald-500`
9. **O2:** Extrair TONE_CONFIG para `src/lib/colorPalette.ts`

### Prioridade BAIXA

10. **m13:** Adicionar testes para `process-jobs`, `ClientContext.tsx`
11. **DS4:** Trocar `animate-pulse` por `animate-shimmer` no Skeleton
12. **DS5:** Adicionar `motion-safe:` prefix nas animações custom

---

## Contagem Final

- **Itens auditados:** 13 checklist + anti-padrões + design system + segurança
- **PASS:** m2, m3, m4, m5, m6, m7, m8, m10, m11 (9/13 checklist limpos)
- **FAIL/PARCIAL:** m1, m9, m12, m13
- **Violações totais:** 4 críticas, 9 médias, 3 baixas
- **Saúde geral do projeto:** BOM — base sólida, violações pontuais e corrigíveis

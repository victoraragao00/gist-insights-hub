# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — .env com VITE_* (chaves publicas) fica no repo
# porque o Lovable precisa para build. NUNCA colocar service_role_key ou
# secrets de IA (GEMINI_API_KEY, CLAUDE_API_KEY) neste arquivo.
.env.local
.env.*.local
.env.production
.env.staging

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
